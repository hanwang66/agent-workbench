# Translation Agent TODO

## 目标

- 从本地可用 Demo 逐步演进到可扩展、可评估、可生产部署的翻译 RAG 系统。

## P0: 稳定性与可用性

- [x] 增加启动自检接口，检查 `OLLAMA_MODEL` 与 `OLLAMA_EMBED_MODEL` 是否可用。
- [x] 增加更明确的错误码和错误分类（模型不可用、embedding 失败、文件编码不支持、超时）。
- [x] 为 `rag_top_k`、文件大小、上传类型添加后端兜底校验和统一提示。
- [x] 增加请求超时与重试策略（可配置重试次数、退避时间）。
- [x] 增加基础单元测试（`split_text`、`search`、`prompt builder`、API schema）。

## P1: RAG 核心增强

- [ ] 接入真正的向量数据库（替代本地 JSON）：
  - [ ] `Chroma`（本地优先，接入简单）
  - [ ] `Qdrant`（可本地可服务化，检索能力强）
  - [ ] `Milvus`（大规模场景）
- [x] 增加文档元数据（source、tags、language、created_at）并支持过滤检索。
- [ ] 增加重排（Rerank）步骤，提高召回后排序质量。
- [ ] 增加混合检索（关键词 BM25 + 向量检索）以提升术语命中率。
- [x] 支持按知识库/项目隔离（`knowledge_base_id`）。
- [x] 增加去重策略（文档哈希、重复 chunk 检测）。

## P1: 文档处理能力

- [ ] 支持 `pdf/docx/pptx` 文档抽取（当前主要是文本文件）。
- [ ] 增加 OCR 通道（扫描件图片转文本）。
- [ ] 增加更智能分块策略（按段落/标题/语义断点分块）。
- [ ] 增加多语言预处理（中英混排清洗、全角半角统一）。

## P1: 翻译质量增强

- [ ] 增加术语表优先级配置（术语表 > RAG > 模型自由翻译）。
- [ ] 增加“风格模板库”（技术文档、商务邮件、客服语气）。
- [ ] 增加领域模板（法律/医疗/金融）及风险提示。
- [ ] 增加回译校验模式（源文 -> 译文 -> 回译，评估偏差）。

## P2: 可观测性与评测

- [ ] 增加结构化日志（请求 ID、耗时、命中 chunk、错误分类）。
- [ ] 增加指标监控（QPS、P95、RAG 命中率、平均 chunk 数）。
- [ ] 增加离线评测集与评分脚本（BLEU/COMET/术语命中率）。
- [ ] 增加前端历史数据导出（CSV）与筛选统计。

## P2: 安全与权限

- [ ] 增加 API Key 或 JWT 鉴权。
- [ ] 增加上传文件病毒扫描或隔离区处理。
- [ ] 增加速率限制（按 IP/Key 限流）。
- [ ] 增加敏感信息脱敏（日志与返回中脱敏）。

## P2: 工程化与部署

- [ ] 增加 Dockerfile 与 `docker-compose` 一键部署。
- [ ] 区分 `dev/staging/prod` 配置。
- [ ] 增加 CI（lint + test + build）。
- [ ] 增加版本化迁移脚本（向量库 schema 升级）。

## P3: 体验升级

- [ ] 前端增加“请求历史导出 CSV”按钮。
- [ ] 前端增加“对比模式”（同一句开/关 RAG 对比结果）。
- [ ] 前端增加“术语命中高亮”展示。
- [ ] 前端增加“知识库管理页”（搜索、删除、批量导入）。

## 建议里程碑

- M1（1-2 周）：P0 + 向量数据库 PoC（优先 Chroma 或 Qdrant）。
- M2（2-4 周）：混合检索 + 重排 + 文档处理增强。
- M3（4-6 周）：评测体系 + 安全鉴权 + 可观测性 + 部署流水线。

## 备注

- 当前系统已具备本地持久化与基础 RAG 闭环，可优先把“向量数据库替换”和“评测指标”作为下一阶段主线。
